# Home Assistant 내장 브리지

## 분석 결과와 구현 범위

참조: [af950833/purethink_bridge](https://github.com/af950833/purethink_bridge/tree/c66ac57aa3705f7ab8bac7256732a08a22415b78), `src/index.js` 및 README.

원본은 Node.js Aedes TLS MQTT 브로커, 제조사 MQTT 중계, 선택적 내부 MQTT 중계,
웹 대시보드, ASUS 라우터 SSH/DNAT 관리로 구성되어 있습니다.
통신 부분은 HA 프로세스 안에서도 구현 가능합니다. 이 통합은 Python aMQTT를 사용해
동등한 핵심 라우팅을 독립적으로 구현하며, 기존 fan/protocol 코드를 그대로 사용합니다.

| 원본 기능 | 이 통합의 구현 |
| --- | --- |
| 기기용 TLS MQTT 수신, 자체 서명 인증서 | 내장 브리지 모드, 기본 TCP 8885 |
| 기기 상태 → 내부 MQTT → HA | 내장 브로커 → 기존 HA 엔티티로 직접 전달 |
| HA 명령 → 기기 | 기존 명령 생성 로직 → 내장 브로커 → 기기 |
| 기기 ↔ 제조사 서버 | 선택적 중계, 자동 재접속, 토픽·페이로드별 5초 에코 제거 |
| 제조사 장애 중 로컬 제어 | 제조사 연결과 독립된 로컬 통신 |
| 웹 설정/대시보드 | HA 설정·재설정 화면 및 기존 엔티티 |
| 별도 내부 MQTT 서버로 동시 복제 | 내장 모드에서는 제공하지 않음. 별도 브리지의 내부 MQTT를 사용할 때는 기존 Local MQTT 모드 사용 |
| 라우터 SSH/DNAT 자동 변경, OTA/펌웨어 패치 | 이식하지 않음. 네트워크와 기기에서 별도 준비 필요 |

## 사용 방법

1. HA에 이 통합을 설치하고 재시작합니다. 테스트 기준은 HA 2025.4.4 / Python 3.13입니다.
2. **설정 → 기기 및 서비스 → Purethink → 추가** 또는 기존 항목의 **재설정**을 엽니다.
3. 기기 ID와 이름을 확인하고 `Embedded TLS bridge` 모드를 선택합니다.
4. TLS 수신 포트(기본 `8885`)와 제조사 중계 여부를 저장합니다.
5. 라우터에서 **해당 기기에서 나오는 제조사 MQTT 트래픽만** HA IP/수신 포트로 DNAT합니다.
   HA 호스트 자체의 제조사 연결까지 되돌리면 중계 루프가 발생합니다.
6. 기기가 연결되면 기존 fan·sensor 등의 상태가 갱신됩니다. `Device ID` 센서는 연결 전에도 설정된 ID를 표시합니다.

기기별 브로커를 사용하므로 여러 기기는 서로 다른 포트(예: 8885, 8886)를 지정하고
기기별 DNAT 대상 포트를 맞춰야 합니다. 설정 화면에서 중복 포트를 검사하며,
외부 프로세스가 점유한 포트는 설정 오류로 표시하고 재시도합니다.
Container 설치는 해당 포트를 호스트에 노출해야 하며, HA OS에서도 호스트까지
트래픽이 도달해야 합니다. 선택한 포트는 모든 IPv4 인터페이스에서 TLS로 수신합니다.

## 펌웨어·네트워크 조건

원본 README는 ESP8266 DIV01 펌웨어의 제조사 인증서 fingerprint 검증을 설명합니다.
**그 검증을 유지하는 기기는 브리지의 자체 서명 인증서를 거부할 수 있습니다.**
호환 펌웨어와 DNAT를 먼저 준비해야 하며, 통합 설치만으로 원래 펌웨어의 검증을
우회하거나 라우터 경로를 바꿀 수는 없습니다. 이 구현은 펌웨어를 변경하지 않습니다.

인증서와 개인키는 HA 설정 디렉터리의 `.storage/purethink_bridge/<entry_id>/`에
생성해 재사용합니다. 키 파일 권한은 `0600`입니다. 하드웨어는 설정된 MQTT client ID로
접속하며, HA 클라이언트는 실행마다 생성한 비밀번호를 사용합니다. MQTT client ID는
강한 기기 인증 수단이 아니므로 이 수신 포트는 신뢰하는 기기 네트워크에서 접근하도록 구성하세요.

제조사 중계를 켜면 원본과 동일하게 제조사 인증서 검증 없이 TLS로 연결합니다.
로컬 전용 운용은 중계를 끄면 됩니다. 제조사에 보내지 못한 상태를 무한 적재하지 않습니다.
기기는 자신의 `/things/<device_id>/...` 토픽만 사용할 수 있고, 다른 기기의 토픽은 거부합니다.

## 재설정과 검증 범위

재설정하면 기존 엔티티 ID를 유지하면서 연결을 종료하고 새 설정으로 다시 연결합니다.
브리지 모드를 나가면 브로커 포트도 해제됩니다. 기기 ID 변경 시 Device ID 센서 값과
엔티티 unique ID도 함께 갱신됩니다.

실제 TLS 소켓과 Paho 기기 에뮬레이터로 상태 수신, HA fan 명령, 포트 해제 및 중계를
검증합니다. 실제 환기장치, 제조사 운영 서버, 사용자의 라우터/펌웨어 조합은 이 개발
환경에서 검증하지 않았습니다. 커버리지 수치는 통합 코드의 실행 범위이며 실기기 호환성 보증이 아닙니다.
