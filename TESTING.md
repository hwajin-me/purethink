# Purethink 검증 결과

## 현재 결과

- macOS / Python 3.13.1 / Home Assistant 2025.4.4 / Paho MQTT 2.1.0
- 실제 Docker Mosquitto 테스트 4개 포함 **146개 통과**
- 이전 브리지 단계의 Linux ARM64 / Python 3.13.15 / HA 2025.4.4 검증: **135개 통과, Docker 중첩 실행 테스트 2개 건너뜀**, 동일한 커버리지 100%
- Linux는 새 컨테이너에서 의존성을 설치하고 검사했으며, 최초 설치에는 HA의 네이티브 의존성 빌드를 위한 C/C++ 도구가 필요했음
- 통합 11개 모듈의 실행 문장 **1,066 / 1,066, 100.00%**
- Paho MQTT 1.6.1에서도 복구 감시·실제 MQTT 테스트 **11개 통과**
- `pyproject.toml`과 릴리스 workflow 모두 커버리지 100% 미달 시 실패
- 커버리지 제외 규칙을 추가하지 않고 실제 TLS 소켓 테스트와 오류 주입 테스트로 검사

## 검증 기준

- 기존 사용자 소스: `d00f47f`
- Purethink 원본: `8d03ed1653a99e5cd8a31b687e19606a005bb786`
- 브리지 원본: `c66ac57aa3705f7ab8bac7256732a08a22415b78`
- Python aMQTT 0.11.3, Docker Mosquitto 2.1.2

## 검증 범위

| 검증 | 내용 |
| --- | --- |
| 패킷 호환성 | 1,152개 상태 × 23개 명령 = 26,496개 조합을 사용자 소스·원본 소스에서 생성한 고정 결과와 비교 |
| 패킷 구조 | 전체 23바이트, 명령 헤더, 체크섬. 무작위 `topic_id`만 비교에서 제외 |
| 기존 fan 로직 | Manual/Auto/Sleep, 0~100% 속도, 켜기·끄기·토글, 전원 복원, 흡배기 보정 조건 |
| HA 플랫폼 | 실제 설정 관리자, 5개 플랫폼, 기기당 18개 엔티티, 서비스 호출, 레지스트리 연결 |
| Device ID 센서 | 최초 텔레메트리 전 ID 표시, 진단 분류, ID 변경 후 기존 entity ID 유지 및 값 갱신 |
| 초기 상태 | 플랫폼 생성 전 수신한 retained 상태도 모든 엔티티에 반영 |
| 실제 MQTT | 인증, 두 기기 격리, retained, CMD 에코 무시, Mosquitto 재시작과 재구독 |
| 내장 TLS 브리지 | 실제 Paho 기기 에뮬레이터 → HA 상태 수신, HA fan 명령 → 기기 |
| 제조사 중계 | 별도 로컬 TLS 테스트 브로커로 양방향 중계, 하위 토픽 전달, 에코 제거 |
| 제조사 연결 장애 | 제조사 테스트 브로커를 종료한 뒤에도 로컬 상태 수신과 fan 제어 유지 |
| 접근 제한 | 실제 TLS 소켓에서 미등록 client ID 거부, `/things/#` 구독 거부, 타 기기 토픽 차단 |
| 브리지 재설정 | 실제 연결에서 포트 변경, 이전 포트 해제, 엔티티 ID 보존, 새 연결 상태 수신 |
| 종료·실패 | HA 종료 이벤트, MQTT CONNECT 전 유휴 TLS 연결, 중복 종료, 진행 중 전송 정리, 인증서/포트 오류, cloud 시작 취소 |
| 설정 흐름 | 제조사/로컬/브리지 선택, 기존 값 복원, 마지막 단계 전 미저장, 모드 변경 시 불필요한 설정 제거 |
| 재구성 충돌 | 중복 ID/포트, 오래된 설정 흐름, unload 대기 중 ID 충돌, 레지스트리 마이그레이션 실패 복구 |
| 입력·연결 오류 | 잘못된 JSON/패킷/속도/압력/포트, MQTT 인증 실패와 수정 후 복구, 송수신·해제 예외 |
| 릴리스 | ZIP 파일 구성·번역 포함·manifest 버전, 소스 불변, HACS 버전 비교, workflow main 트리거·테스트 의존성 |

패킷 비교의 SHA-256 기준은 `tests/fixtures/protocol_hashes.json`에 원본 커밋별로 고정되어
있습니다. 현재 구현을 실행해 기대값을 다시 만드는 테스트가 아닙니다. 기존 fan 로직의
명령 바이트가 브리지 모드에서도 그대로 전달됩니다.

## 주요 수정 사항

- `fan.toggle` 키워드 인자 전달, 초기 수신 상태 반영, 고장 비트 통합
- 최신 상태를 사용하는 취소 가능한 1초 흡배기 보정 타이머
- 기기별 MQTT 연결과 명령 토픽 분리, 제품 ID 중복 차단
- MQTT CONNACK까지 대기해 인증 실패를 설정 실패로 반영
- 연결 중 취소될 때 executor 작업 종료 후 자원 해제
- 모든 재설정에서 unload 성공 확인, 레지스트리 변경 실패 시 복구
- Python 3.13 서버 종료 시 활성 MQTT/TLS 연결을 먼저 닫아 포트 해제 대기 방지
- HA 종료와 entry unload가 중첩되어도 브리지 정리를 안전하게 반복
- aMQTT 0.11.3의 QoS 1 전송 취소가 브로커 종료를 중단시키는 문제를 보완하고 실제 호출자 취소는 유지

## 실행 방법

```sh
python -m pip install -r requirements.txt 'homeassistant==2025.4.4'
python -m pytest --cov=custom_components.purethink --cov-report=term-missing
```

실제 Mosquitto까지 포함:

```sh
docker pull eclipse-mosquitto:2
PURETHINK_LIVE_MQTT=1 python -m pytest --cov=custom_components.purethink --cov-report=term-missing --cov-report=html
```

HTML 보고서는 `htmlcov/index.html`에 생성됩니다. 기본 실행에서는 Docker가 필요한
4개 테스트만 건너뛰며, **내장 TLS 브리지 테스트는 기본 실행에도 포함**됩니다.
테스트는 임시 포트·인증서·테스트용 계정과 별도 브로커를 사용하고 정리합니다.
제조사 운영 서버나 실제 환기장치·라우터에는 연결하지 않습니다.

## 한계

100%는 **실행 문장(statement) 커버리지**이며 분기(branch) 100%나 모든 동작의 정확성을
보장한다는 의미가 아닙니다. GitHub workflow는 로컬에서 구성·패키지를 검사했으며,
원격 push나 실제 GitHub Release 게시를 실행하지 않았습니다.

실제 제품 펌웨어, 제조사 운영 서버, 사용자 라우터 DNAT, 다른 HA 버전은 검증하지
않았습니다. 브리지 운용 조건과 원본 대비 구현 범위는 [BRIDGE.md](BRIDGE.md)에 있습니다.
저장 후 새 연결이 실패하면 새 설정은 남아 있으므로 재설정에서 수정할 수 있습니다.


## MQTT 재시도 보완

최초 연결을 `connect_async()`와 네트워크 스레드로 시작하고 재접속 간격을 5~60초로
명시했습니다. 연결 대기 시간 초과·인증 거부 시에는 HA의 설정 재시도가 계속됩니다.
연결 후에는 60초마다 MQTT 연결과 통신 스레드를 검사하고, 두 번 연속 비정상이면
해당 entry를 재로드해 새 연결을 만듭니다. 재로드 실패 시에도 감시를 중단하지 않습니다.

Paho 1.6과 2.x 모두 통신 스레드 종료 후 `is_connected()`가 참으로 남을 수 있으므로,
두 지원 버전의 `_thread` 참조와 `is_alive()`도 검사합니다. 관련 공개 건강 상태 API가
없어 이 내부 속성에 대한 의존성을 두 버전의 실제 MQTT 테스트로 검증했습니다.

추가 검증:

- 브로커가 꺼진 상태에서 최초 설정이 두 번 이상 실패한 뒤, 브로커만 켜서 자동 복구
- 실제 HA의 재시도 예약 실행, LOADED 상태 복귀, 토픽 재구독과 fan 상태 수신
- 연결 플래그가 참인 상태에서 통신 스레드를 종료한 뒤 새 클라이언트로 복구
- 일시적 끊김은 정상 Paho 재접속을 허용하고, 회복하면 실패 누적을 초기화
- 재로드가 완료되지 않아도 다음 검사에서 재시도
- 다른 기기에 영향 없음, 교체·삭제된 연결의 타이머 무효화, 종료·해제 시 취소

최종 결과는 Paho 2.1.0 전체 146개 통과 및 1,066개 실행 문장 100%,
Paho 1.6.1 복구 감시·실제 MQTT 11개 통과입니다. 이번 재시도 변경의 Linux 재실행은
하지 않았으며, 위 Linux 수치는 이전 브리지 단계의 검증 결과입니다.
